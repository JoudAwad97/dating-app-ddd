export interface DislikeProps {
  sourceProfileId: string;
  targetProfileId: string;
  reason: string;
}

export interface DislikeCreateProps {
  sourceProfileId: string;
  targetProfileId: string;
  reason: string;
}
